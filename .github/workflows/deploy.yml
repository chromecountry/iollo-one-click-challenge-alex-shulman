name: One-Click Deploy
# Updated to ensure workflow_dispatch trigger is recognized

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      customer:
        description: 'Customer ID'
        required: true
        default: 'demo-customer'
      region:
        description: 'AWS Region'
        required: true
        default: 'us-east-1'
      size:
        description: 'Deployment size'
        required: true
        default: 'small'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      # TODO: Add validation steps
      - run: echo "TODO: Terraform fmt and validate"

  plan:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      # TODO: Add plan steps
      - run: echo "TODO: Terraform plan"

  deploy:
    runs-on: ubuntu-latest
    needs: plan
    steps:
      # TODO: Add dry-run deploy steps
      - run: echo "TODO: Deploy infrastructure (dry-run)"

  test:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      # TODO: Add smoke test steps
      - run: echo "TODO: Run smoke checks"
